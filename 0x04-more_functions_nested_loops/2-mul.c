#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: multiplication (a*b)
 */
int mul(int a, int b)
{
	int mul;

	mult = a * b;
	return (a * b);
}
