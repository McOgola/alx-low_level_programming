#include <stdio.h>

/**
 * main - computes and prints all multiples of 3 and 5
 * Return:  sum of the multiples of 3 and 5 except 1024
 */
int main(sum)
{
	int sum;

	sum_of_special numbers = 0
	{
		for (1, 1024)
		{
			
		if (number % 3 == 0 or number % 5 == 0);
		{
		_putchar(sum_of_special_numbers('\n');
		}
		return (sum_of_special_numbers);
		}
	}
}
